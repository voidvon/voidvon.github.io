# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Common Commands
- **Development**: Run `npm run dev` to start the local server for development.
- **Build**: Use `npm run build` to create production-ready files.
- **Preview**: To preview the built files, run `npm run preview`.

## Code Structure
This codebase primarily uses VitePress for static site generation. The main structure includes:
- **src/**: Contains source code, including documentation and assets. The `docs/` folder holds markdown files that form the documentation.
- **node_modules/**: Contains project dependencies.

There are no additional configurations or rules found in `.cursor/rules/` or `Copilot instructions` files.